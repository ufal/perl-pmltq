---
data_dir: "data"
resources: "resources" # main directory with PML schemas

db:
  name: mschema_test
  host: localhost
  port: 15432
  user: postgres
  password:

sys_db: postgres

layers: # description of all data layers
  - name: adata
    data: *.a.gz
    references:
      a-root/s.rf: '-'
      m-node/id: '-'
      m-node/src.rf: '-'
      w-node/id: '-'
